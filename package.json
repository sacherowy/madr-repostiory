{
  "name": "adr-manager",
  "private": true,
  "packageManager": "pnpm@9.7.0",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:integration": "pnpm -r test:integration",
    "e2e": "pnpm --filter @adr/e2e test:e2e",
    "reindex": "pnpm --filter @adr/api reindex"
  }
}
